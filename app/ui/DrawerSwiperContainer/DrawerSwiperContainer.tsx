import { styled } from '@mui/material';
import { Swiper } from 'swiper/react';

const DrawerSwiperContainer = styled(Swiper)(() => ({
  width: '100%',
  height: '400px',
}));

export default DrawerSwiperContainer;
